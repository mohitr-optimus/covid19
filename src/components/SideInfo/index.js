import React from 'react';
import SideInfoJSX from './SideInfo';

export default function SideInfo({ upText, center, downText }) {
	return <SideInfoJSX upText={upText} center={center} downText={downText} />;
}
